# knox-multitenant-demo

To build knox docker image

docker build -t sumitg/knox-mten .

To build loans app image

docker build -t sumitg/loans-app .


To run it all:

docker-compose up -d

to shut it down

docker-compose down

